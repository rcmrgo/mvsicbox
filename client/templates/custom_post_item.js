/*Template.posts_item.onRendered(function () {
  //this.findAll('.post iframe');
  $('.post iframe').each(function(i, e){
      var player = new playerjs.Player(e);
      players.push(player);
      player.on('ready', function(){
        player.unmute();
        onReady();
      });
    });
  console.log('list rendered');
});*/